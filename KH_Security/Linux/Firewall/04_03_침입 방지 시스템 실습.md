<img width="479" height="121" alt="45" src="https://github.com/user-attachments/assets/7ac9906f-a21d-44bf-8824-8d3973327f43" /># 침입 탐지 시스템 실습

## 개요

- Snort를 이용하여 다양한 공격 및 접근 시도를 탐지하는 실습입니다.
- 실제 패킷을 발생시키고 Rule을 직접 작성하여 alert 로그를 확인합니다.
- Payload, Non-Payload, flow, threshold를 이용해 세밀한 탐지 정책을 구성합니다.
- 단순 Ping 탐지부터 Scan, Flooding, Land Attack까지 실제 공격 형태를 실습합니다.

---

## Option - payload 

### Rule 설정

- `vi /etc/snort/rules/local.rules` 파일에 다음과 같이 Rule을 설정합니다.

![42](/KH_Security/Linux/Firewall/img/42.png)

---

### HTTP GET 탐지

- 다음과 같은 Rule 설정으로 HTTP 요청 중 GET 문자열을 탐지합니다.
```
alert tcp any any -> any 80 (msg:"HTTP GET Detect"; content:"GET"; sid:1000105; rev:1;)
```

- 목적지 80번 포트(HTTP)로 가는 TCP 패킷 중 payload에 GET 문자열이 포함되면 탐지합니다.
- 웹 접속 요청(HTTP GET)을 탐지하는 Rule입니다.

#### 결과 확인

- 192.168.11.19 에서 다음 명령어를 입력합니다.
```
curl http://192.168.12.11
```

![43](/KH_Security/Linux/Firewall/img/43.png)

- alert 로그에서 HTTP GET Detect가 발생한 것을 확인할 수 있습니다.
- 출발지 192.168.11.3에서 목적지 192.168.12.11의 80번 포트로 접속한 것을 의미합니다.
- payload 내부에 GET 문자열이 포함되어 있어 Rule이 정상적으로 탐지되었습니다.

---

### FTP ROOT Detect

- 다음과 같은 Rule 설정으로 목적지 21번 포트(FTP)로 가는 TCP 패킷을 검사합니다.
```
alert tcp any any -> any 80 (msg:"HTTP GET to Server"; flow:to_server,established; content:"GET"; sid:1000301; rev:1;)
```

- payload에 USER root 문자열이 포함되면 탐지합니다.
- nocase 옵션이 있으므로 대소문자를 구분하지 않고 탐지합니다.
- 즉, FTP 접속 과정에서 root 계정 로그인을 시도하는지 확인하는 Rule입니다.

#### 결과 확인

- 192.168.11.19에서 다음 명령어로 ftp 접속을 시도합니다.
```
ftp 192.168.12.11
```

![44](/KH_Security/Linux/Firewall/img/44.png)

- alert 로그에서 AP FTP Detect가 발생한 것을 확인할 수 있습니다.
- 출발지 192.168.11.19에서 목적지 192.168.12.11의 21번 포트로 FTP 접속한 것을 의미합니다.
- 21번 포트는 FTP 제어 연결 포트이므로 FTP 접속 시도 또는 로그인 관련 트래픽이 탐지된 상태입니다.
- 즉, EXTERNAL 서버에서 AP 서버의 FTP 서비스로 접속하는 트래픽을 Snort가 정상적으로 탐지한 상태입니다.

---

### PHF Offset Depth Detect

- content 옵션으로 `cgi-bin/phf` 문자열을 탐지합니다.
```
alert tcp any any -> any 80 (msg:"PHF Offset Depth Detect"; content:"cgi-bin/phf"; offset:4; depth:20; sid:1000107; rev:1;)
```

- 즉, payload의 4byte부터 20byte 범위 안에서만 `cgi-bin/phf` 문자열을 검사합니다.

- 불필요하게 전체 패킷을 검사하지 않고 특정 위치만 확인하여 탐지 정확도를 높이는 Rule입니다.

#### 결과 확인

- 다음 명령어로 웹 서버에 PHF 취약점 경로로 요청을 보냅니다.
```
curl http://192.168.12.11/cgi-bin/phf
```

![45](/KH_Security/Linux/Firewall/img/45.png)

![46](/KH_Security/Linux/Firewall/img/46.png)

- alert 로그에서 PHF Offset Depth Detect가 발생한 것을 확인할 수 있습니다.
- 출발지 192.168.11.19에서 목적지 192.168.12.11의 80번 포트로 요청이 전달되었습니다.
- payload 내부에 `cgi-bin/phf` 문자열이 포함되어 있어 Rule이 정상적으로 탐지되었습니다.

---

### DISTANCE TEST

- 다음과 같은 Rule 설정으로 payload 내부의 문자열 순서와 거리를 검사합니다.
```
alert tcp any any -> any any (msg:"DISTANCE TEST"; content:"ABC"; content:"DEF"; distance:1; sid:1000108; rev:1;)
```

- 먼저 `ABC` 문자열을 탐지합니다.
- 이후 distance:1 옵션을 통해 1byte 뒤부터 `DEF` 문자열을 다시 탐지합니다.
- 즉, `ABC` 다음에 일정 거리 이후 `DEF`가 존재하는지 확인하는 Rule입니다.

#### 결과 확인

- 192.168.11.19에서 다음 명령어로 테스트 패킷을 전송합니다.
```
printf 'GET /ABCXDEF HTTP/1.1\r\nHost: 192.168.12.11\r\n\r\n' | nc 192.168.12.11 80
```

- `ABCXDEF` 형태로 문자열이 포함된 HTTP 요청을 전송합니다.

- IDS 서버에서 다음 명령어로 alert 로그를 확인합니다.
```
cat /var/log/snort/alert
```

![47](/KH_Security/Linux/Firewall/img/47.png)

- alert 로그에서 DISTANCE TEST가 발생한 것을 확인할 수 있습니다.
- `ABC` 이후 1byte 뒤에 `DEF`가 존재하므로 Rule이 정상적으로 탐지되었습니다.
- 즉, 문자열 간의 위치 관계를 기준으로 공격 패턴을 탐지한 실습입니다.

---

## FTP ROOT 탐지

alert tcp any any -> any 21 (msg:"FTP ROOT"; content:"USER root"; nocase; sid:1000106; rev:1;)

- FTP 로그인 시 root 계정 접속을 탐지합니다.

### 테스트

ftp 192.168.12.11

### 결과 확인

- FTP ROOT alert가 발생합니다.

![42]()

---

## FTP Login Success 탐지

alert tcp any 21 -> any any (msg:"FTP Login Success"; flow:to_client,established; content:"Login successful"; sid:1000302; rev:1;)

- FTP 서버 → 클라이언트 방향의 로그인 성공 응답을 탐지합니다.

### 결과 확인

- FTP Login Success alert가 발생합니다.

![56]()

![57]()

---

## SSH 탐지

alert tcp any any -> 192.168.12.11 22 (msg:"AP SSH Detect"; sid:1000005; rev:1;)

- SSH 22번 포트 접속을 탐지합니다.

### 결과 확인

- AP SSH Detect alert가 발생합니다.

![55]()

---

## PHF Offset Depth 탐지

alert tcp any any -> any 80 (msg:"PHF Offset Depth Detect"; content:"cgi-bin/phf"; offset:4; depth:20; sid:1000107; rev:1;)

- payload의 특정 위치만 검사하여 공격 문자열을 탐지합니다.

### 테스트

curl http://192.168.12.11/cgi-bin/phf

### 결과 확인

- PHF Offset Depth Detect alert가 발생합니다.

![43]()

![44]()

---

## Distance 탐지

alert tcp any any -> any any (msg:"DISTANCE TEST"; content:"ABC"; content:"DEF"; distance:1; sid:1000108; rev:1;)

- 첫 번째 문자열 이후 일정 거리 뒤에서 다음 문자열을 탐지합니다.

### 결과 확인

- DISTANCE TEST alert가 발생합니다.

![45]()

---

## ICMP Redirect 탐지

alert icmp any any -> any any (msg:"ICMP Redirect"; itype:5; sid:1000201; rev:1;)

- ICMP Type 5 (Redirect)를 탐지합니다.

### 테스트

hping3 --icmp --icmptype 5 192.168.12.11

### 결과 확인

- ICMP Redirect alert가 발생합니다.

![46]()

![47]()

---

## ICMP Backdoor 탐지

alert icmp any any -> any any (msg:"ICMP Backdoor : ls"; itype:8; icode:0; content:"ls"; sid:1000202; rev:1;)

- ICMP payload 내부의 ls 문자열을 탐지합니다.

### 테스트

echo "ls" > payload.txt

hping3 --icmp -d 20 -E payload.txt 192.168.12.11

### 결과 확인

- ICMP Backdoor : ls alert가 발생합니다.

![48]()

![49]()

---

## ICMP Ping 탐지

alert icmp any any -> any any (msg:"ICMP Ping"; sid:1000201; rev:1;)

- 일반적인 Ping(ICMP Echo Request)을 탐지합니다.

### 테스트

ping 192.168.12.11

### 결과 확인

- ICMP Ping alert가 발생합니다.

![69]()

---

## ICMP Timestamp 탐지

alert icmp any any -> any any (msg:"ICMP Timestamp"; itype:13; sid:1000202; rev:1;)

- ICMP Timestamp Request를 탐지합니다.

---

## Land Attack 탐지

alert icmp any any -> 192.168.11.0/24 any (msg:"Land Attack Detect"; sameip; sid:2000001; rev:1;)

- source IP와 destination IP가 같은 패킷을 탐지합니다.

### 테스트

hping3 -a 192.168.12.11 192.168.12.11 --icmp

### 결과 확인

- Land Attack Detect alert가 발생합니다.

![50]()

![51]()

![70]()

---

## XMAS Scan 탐지

alert tcp any any -> any any (msg:"XMAS Packet Detected"; flags:FPU; flow:stateless; sid:1000101; rev:1;)

- FIN + PSH + URG 조합의 XMAS Scan을 탐지합니다.

### 결과 확인

- XMAS Packet Detected alert가 발생합니다.

![52]()

---

## NULL Scan 탐지

alert tcp any any -> any any (msg:"NULL Packet Detected"; flags:0; sid:1000205; rev:1;)

- TCP Flag가 없는 NULL Scan을 탐지합니다.

### 결과 확인

- NULL Packet Detected alert가 발생합니다.

![53]()

---

## FIN Scan 탐지

alert tcp any any -> any any (msg:"FIN Scan Detected"; flags:F; flow:stateless; sid:1000102; rev:1;)

- FIN Flag만 사용하는 FIN Scan을 탐지합니다.

### 결과 확인

- FIN Scan Detected alert가 발생합니다.

![54]()

![71]()

---

## SYN Scan 탐지

alert tcp any any -> any any (msg:"SYN Scan Detected"; flags:S; flow:stateless; threshold:type threshold, track by_dst, count 5, seconds 2; sid:1000103; rev:1;)

- SYN 패킷이 짧은 시간 안에 반복 발생하면 탐지합니다.

### 결과 확인

- SYN Scan Detected alert가 발생합니다.

![72]()

---

## Threshold ICMP 탐지

alert icmp any any -> any any (msg:"[THRESHOLD] ICMP Echo Request"; itype:8; threshold:type threshold, track by_dst, count 3, seconds 10; sid:2001001; rev:1;)

- 같은 목적지로 일정 횟수 이상 Ping이 발생할 때 탐지합니다.

### 테스트

ping -c 10 192.168.12.11

### 결과 확인

- threshold 조건이 만족되면 alert가 발생합니다.

![63]()

![64]()

---

## SYN Flooding 탐지

alert tcp any any -> any 80 (msg:"ATT : SYN Flooding"; flags:S; threshold:type threshold, track by_dst, count 100, seconds 1; sid:2000002; rev:1;)

- 짧은 시간에 SYN 패킷이 대량 발생하면 탐지합니다.

### 결과 확인

- ATT : SYN Flooding alert가 발생합니다.

![73]()

---

## UDP Flooding 탐지

alert udp any any -> any any (msg:"ATT : UDP Flooding"; threshold:type threshold, track by_dst, count 100, seconds 1; sid:2000003; rev:1;)

- UDP 패킷이 대량 발생하는 Flooding 공격을 탐지합니다.

### 결과 확인

- ATT : UDP Flooding alert가 발생합니다.

![74]()

---

## ICMP Flooding 탐지

alert icmp any any -> any any (msg:"ATT : ICMP Flooding"; threshold:type threshold, track by_dst, count 100, seconds 1; sid:2000004; rev:1;)

- ICMP Flooding 공격을 탐지합니다.

### 결과 확인

- ATT : ICMP Flooding alert가 발생합니다.

![68]()

---

## 요약 정리

- content는 문자열 기반 탐지
- nocase는 대소문자 무시
- offset/depth는 탐색 범위 제한
- distance는 content 간 거리 조건
- itype/icode는 ICMP 공격 탐지
- sameip는 Land Attack 탐지
- flags는 TCP Scan 탐지
- flow는 방향과 세션 상태 확인
- threshold는 반복 공격 탐지 제어
- flooding은 짧은 시간 내 대량 트래픽 탐지

즉, Snort는 단순 패킷 감지가 아니라 실제 공격 패턴을 정밀하게 탐지하는 IDS입니다.
